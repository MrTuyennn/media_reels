import 'dart:ui';

class L10n {
  static final all = [
    const Locale('en', 'US'),
    const Locale('vi', 'VN'),
  ];
}

class AppConfig {
  static const Locale defaultLanguage = Locale('en', 'US');
}
